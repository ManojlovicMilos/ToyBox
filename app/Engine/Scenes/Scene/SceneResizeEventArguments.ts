import * as Math from '../../../Mathematics/Mathematics';

type SceneResizeEventArgumants = {
    ratio: number,
    cameraScale: Math.Vertex,
    globalScale: Math.Vertex,
}

export default SceneResizeEventArgumants;
